const superHeros: string[] = [];

superHeros.push('SpiderMan');

const heroPower: Array<number> = [];

type User = {
	name: string;
	email: string;
};

const allUsers: User[] = [];
allUsers.push({ name: 'Mustafa', email: 'mus@gmail.com' });
